extends base

block content
    h1 Log In
    form(action="/login", method="post") 
        input(type="text", name="email", placeholder="email", required, maxlenth=100)
        input(type="password", name="password", placeholder="password", required, maxlenth=100)
        input(type="submit", value="logIn")